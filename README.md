# js-articles-recommender-widget

## Usage
```javascript
var Widget = require("articles-recommander-widget");
var widget = new Widget({"host":"example.com"});
widget.loadSlots([{"target": "target", "count": 4}]);
```
